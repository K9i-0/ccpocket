• Keep Settings, Gallery, New Session, and Bridge disconnect controls available while recent sessions load, with a clearer skeleton loading state.
• Cancel in-flight Bridge and SSH auto-connections when disconnecting, so the app reliably returns to the machine list.
